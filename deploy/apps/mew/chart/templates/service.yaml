apiVersion: v1
kind: Service
metadata:
  name: {{ include "mew.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mew.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mew.selectorLabels" . | nindent 4 }}
